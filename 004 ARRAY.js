/*/ Una variable puede conter varios elementos, entonces le llamaremos "array" 
    
  Dentro de el array la posición número 1 corresponde al 0 y así sucesivamente*/

var answer =  ([ 'yes', 'no', 'maybe','sure']);

print(answer);


/*/ Un array inicia con un "[" y termina con un "]", 
    sin importar si los elementos están dispuestos de 
    forma horizontal o vertical */
var ingredients = [
  'sugar',
  'eggs',
  'milk',
  'butter',];

print (ingredients);
